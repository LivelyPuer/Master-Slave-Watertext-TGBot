# Telegram Master-Slave Bot System

Система ботов для добавления водяных знаков на изображения с увеличением разрешения.

## 📋 Возможности

### Master Bot
- Авторизация по паролю
- Создание и управление slave ботами
- Просмотр списка активных slave ботов
- **💾 Автоматическое сохранение slave ботов в базу данных**
- **🔄 Автоматическое восстановление ботов после перезагрузки**

### Slave Bot
- Прием изображений в исходном качестве (как файл)
- Увеличение разрешения изображения в 2 раза
- Добавление полупрозрачного водяного знака справа снизу
- Отправка обработанного изображения обратно пользователю

## 🚀 Установка

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Скачайте шрифт Roboto

Скачайте файл `Roboto.ttf` и поместите его в корень проекта:
- [Скачать Roboto с Google Fonts](https://fonts.google.com/specimen/Roboto)
- Или скачайте напрямую: [Roboto-Regular.ttf](https://github.com/google/roboto/raw/main/src/hinted/Roboto-Regular.ttf)

Структура проекта должна выглядеть так:
```
WaterBot/
├── main.py
├── Roboto.ttf          ← Поместите шрифт здесь
├── .env
├── requirements.txt
└── slaves_database.json (создается автоматически)
```

### 3. Создайте ботов в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Создайте master бота:
   - Отправьте `/newbot`
   - Введите имя бота
   - Введите username бота (должен заканчиваться на `bot`)
   - Скопируйте токен
3. Создайте один или несколько slave ботов таким же способом

### 4. Настройте .env файл

Создайте файл `.env` в корневой директории:

```env
MASTER_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
MASTER_PASSWORD=my_secure_password
```

### 4. Запустите бота

```bash
python main.py
```

## 📖 Использование

### Работа с Master Bot

1. **Авторизация**
   - Отправьте `/start` master боту
   - Введите пароль из `.env`

2. **Создание slave бота**
   - Отправьте `/create_slave`
   - Введите токен slave бота
   - Введите текст водяного знака
   - Slave бот автоматически запустится

3. **Просмотр активных ботов**
   - Отправьте `/list_slaves`

### Работа со Slave Bot

1. Откройте созданный slave бот
2. Отправьте `/start` для инструкций
3. Отправьте изображение **как файл** (не как фото!)
   - Нажмите на скрепку 📎
   - Выберите "Файл"
   - Выберите изображение
4. Получите обработанное изображение с:
   - Увеличенным разрешением x2
   - Водяным знаком справа снизу

## ⚙️ Технические детали

- **Асинхронная работа**: использует `aiogram` 3.x и `asyncio`
- **Обработка изображений**: библиотека `Pillow`
- **Алгоритм масштабирования**: NEAREST (идеально для pixel art - сохраняет четкие пиксели без размытия)
- **Шрифт водяного знака**: Roboto.ttf (современный, читаемый шрифт от Google)
- **Размер водяного знака**: динамический - 30% от минимальной стороны изображения
- **Отступы**: адаптивные - 5% от размера изображения
- **База данных**: JSON файл `slaves_database.json` для хранения slave ботов
- **Автовосстановление**: все slave боты автоматически восстанавливаются после перезапуска
- **Формат водяного знака**: белый текст с 50% прозрачностью
- **Качество выходных изображений**: JPEG с качеством 95%
- **Поддерживаемые форматы**: JPEG, PNG, и другие форматы изображений

### 📐 Как работает динамический размер:

Для изображения 1000x500 пикселей:
- Минимальная сторона = 500px
- Размер водяного знака = 500 × 0.3 = 150px
- Отступы = 5% от размеров (50px по ширине, 25px по высоте)

Для изображения 300x300 пикселей:
- Минимальная сторона = 300px  
- Размер водяного знака = 300 × 0.3 = 90px
- Отступы = 5% от размеров (15px с каждой стороны)

Это гарантирует, что водяной знак всегда выглядит пропорционально, независимо от размера изображения!

## 📁 Структура базы данных

Файл `slaves_database.json` создается автоматически и имеет следующую структуру:

```json
[
  {
    "token": "123456789:ABCdefGHIjklMNOpqrsTUVwxyz123456789",
    "watermark": "© MyCompany 2024"
  },
  {
    "token": "987654321:ZYXwvuTSRqponMLKjihGFEdcba987654321",
    "watermark": "@my_channel"
  }
]
```

- При создании нового slave бота он автоматически добавляется в базу
- При запуске master бота все slave боты из базы восстанавливаются
- Файл обновляется при каждом добавлении нового slave бота

## 🔒 Безопасность

- Доступ к master боту защищен паролем
- Токены ботов хранятся в `.env` файле
- Не коммитьте `.env` в git (добавьте в `.gitignore`)

## 📝 Примечания

- Slave боты работают одновременно с master ботом
- Каждый slave бот может иметь свой уникальный водяной знак
- Изображения должны отправляться как файлы, а не как фото, чтобы сохранить качество
- Для остановки всех ботов остановите процесс Python (Ctrl+C)

## 🐛 Устранение неполадок

**Ошибка "Invalid token"**
- Проверьте правильность токена в `.env`
- Убедитесь, что токен получен от @BotFather

**Бот не отвечает**
- Проверьте подключение к интернету
- Убедитесь, что процесс запущен
- Проверьте логи на наличие ошибок

**Плохое качество водяного знака**
- Убедитесь, что отправляете файл, а не сжатое фото
- Проверьте, что исходное изображение имеет достаточное разрешение

**Ошибка "Roboto.ttf not found"**
- Скачайте шрифт Roboto.ttf
- Поместите его в корневую папку проекта
- Или укажите другой путь к шрифту в коде

**Водяной знак слишком большой/маленький**
- Размер автоматически подстраивается под изображение (30% от минимальной стороны)
- При необходимости измените коэффициент `0.3` в коде на другое значение (например, `0.2` для 20%)